export default {
  /**
   * State do módulo para gerenciar dados do candidatos.
   * @typedef {object} CandidatosState
   * @property {Array} candidatos Lista de candidatos disponíveis.
   */
  candidatos: [],
}
